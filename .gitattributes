# Normalize line endings so Windows/Mac/Linux teammates don't create noisy
# diffs. Git stores LF; checks out the OS-appropriate ending automatically.
* text=auto

# Treat these as binary (never touch line endings).
*.pdf binary
*.mp4 binary
*.png binary
*.jpg binary
*.pt  binary
