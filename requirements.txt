# C.O.A.S.T. - core dependencies (CPU-friendly)
# Pin major versions so every teammate gets the same setup.

ultralytics==8.3.0      # YOLO models + tracking (pulls in torch, opencv, numpy)
opencv-python==4.10.0.84 # video reading, drawing, display
numpy==1.26.4            # arrays / math

# --- Optional, add later when you build alerting ---
# python-dotenv==1.0.1   # load API keys from .env
# requests==2.32.3       # Telegram bot alerts
# streamlit==1.38.0      # polished web dashboard for judges
